{% extends 'base.html.twig' %}

{% block title %}
    Products
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            {% for product in products %}
                <div class="col-xs-4">{{ product }}</div>
            {% endfor %}
        </div>
    <div class="container">
{% endblock %}